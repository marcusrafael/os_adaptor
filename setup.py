from setuptools import find_packages, setup

setup(
    name='os-adaptor',
    version='1.0',
    description='',
    # long_description=long_description,
    author='Ioram Sette',
    author_email='iss@cin.ufpe.br',
    packages=find_packages(exclude=('tests',)),
    install_requires=[]
)
